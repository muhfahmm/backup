{
  "name": "presiden-simulator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"frontend,gateway,heart\" -c \"cyan,magenta,red\" \"npm run dev:frontend\" \"npm run dev:gateway\" \"npm run dev:orchestrator\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:gateway": "npm run dev -w gateway",
    "dev:orchestrator": "nodemon --watch apps/frontend/src/server --ext go --exec \"cd apps/frontend/src/server && go run map_server_rendering.go\"",
    "dev:all": "concurrently -n \"CORE,POLYGLOT\" -c \"bgMagenta.bold,bgCyan.bold\" \"npm run dev\" \"npm run polyglot:start\"",
    "polyglot:start": "node launcher.js",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
