const fs=require('fs').promises; const path=require('path'); (async()=>{ let base=path.join(process.cwd(),'json','semua_fitur_negara'); try{ await fs.access(base); console.log('base exists',base);}catch(e){ const alt=path.join(process.cwd(),'..','json','semua_fitur_negara'); try{ await fs.access(alt); console.log('base found at alt',alt); }catch(e2){ console.error('not found at',base,alt);} }})();